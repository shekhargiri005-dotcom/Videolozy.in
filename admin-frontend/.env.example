# The base URL of the Flask backend API
# In development: leave empty (Vite proxy handles it)
# In production: set to your backend URL e.g. https://api.videolozy.in
VITE_API_BASE_URL=

# Cloudinary cloud name (for building preview URLs client-side)
VITE_CLOUDINARY_CLOUD_NAME=your_cloud_name
